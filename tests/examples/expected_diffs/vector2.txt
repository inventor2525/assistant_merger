--- a/vector2.py
+++ b/vector2.py
@@ -1,3 +1,4 @@
+import math
 class Vector2:
     def __init__(self, x, y):
         self.x = x
@@ -5,6 +6,7 @@ class Vector2:
 
     def add(self, other):
         return Vector2(self.x + other.x, self.y + other.y)
+        # Optimized addition
 
     def magnitude(self):
-        return (self.x ** 2 + self.y ** 2) ** 0.5
+        return math.sqrt(self.x ** 2 + self.y ** 2)
@@ -15,3 +17,7 @@ class Vector2:
     def normalize(self):
         mag = self.magnitude()
         if mag == 0:
@@ -22,3 +26,7 @@ class Vector2:
 
     def dot(self, other):
         return self.x * other.x + self.y * other.y
+
+    def subtract(self, other):
+        return Vector2(self.x - other.x, self.y - other.y)